/*
sio : import(std.stdio)

hello : fn()
	name : var
	do
		sio.println("What your name? ")
		name = sio.getline()
	.. while(name.isNotEmpty())

	sio.printfln("hello, %s", name)


aaa: fn(a, b, c)

*/
